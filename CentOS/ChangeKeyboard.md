# Change Keyboard in CentOS

Run `sudo yum install kbd`.  Should already be installed with CentOS 7.

Run `localectl list-keymaps` to show keyboard layouts.

Run `sudo loadkeys dvorak` to change keyboard to Dvorak.
